/**
 * Placeholder — the screen itself is T-010.
 *
 * It exists so the menu item is not a dead link and so the route group of
 * overview §2 is real.
 */
export default function Page() {
  return (
    <div className="space-y-3">
      <h1>Розклад</h1>
      <p className="text-muted-foreground">Тут з’явиться тижневий шаблон розкладу — окремо для чисельника й знаменника.</p>
    </div>
  );
}
