/**
 * Placeholder — the screen itself is T-009.
 *
 * It exists so the menu item is not a dead link and so the route group of
 * overview §2 is real.
 */
export default function Page() {
  return (
    <div className="space-y-3">
      <h1>Навчальний рік</h1>
      <p className="text-muted-foreground">
        Тут з’являться межі року, семестри, канікули, розклад дзвінків і точки відліку парності.
      </p>
    </div>
  );
}
