# Next.js
.next/
out/
next-env.d.ts

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env files
.env
.env*.local
.env.development
.env.production

# TypeScript
*.tsbuildinfo

# Drizzle
# Nothing here: the whole drizzle/ folder, meta/ snapshots included, is
# committed. drizzle-kit diffs the latest snapshot to generate the next
# migration, so an ignored meta/ makes every generate a full-schema one.

# Docker / Postgres
pgdata/
postgres-data/

# Build / misc
dist/
build/
coverage/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Vercel (if ever used)
.vercel
docs/specs/input/
