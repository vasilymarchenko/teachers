---
id: T-004
type: ticket
title: Drizzle schema, first migration, and the overlap constraint test
status: todo
depends_on: [T-002, T-003]
refs:
  - docs/architecture/design/schema.md
  - docs/architecture/architect-overview.md §3.2
  - docs/architecture/architect-overview.md §4
  - docs/architecture/architect-overview.md §8.4
---

## Goal

Translate the T-003 document into `lib/db/schema` (one file per aggregate) and
generate the first migration, with the `ScheduleTemplate` overlap constraint
proven by a test that talks to a real Postgres.

## Acceptance criteria

- [ ] `lib/db/schema/*.ts`, one file per aggregate, matching T-003 exactly.
- [ ] better-auth's tables generated into `lib/db/schema/auth.ts` and included in
      the same first migration: every profile table has a foreign key to
      `user(id)`, so `user` cannot wait for T-006
      (`docs/architecture/design/schema.md` §5.1). `requireUser()` and the
      sign-in flow stay in T-006.
- [ ] First migration generated by `drizzle-kit` and applied to a clean
      database, including the `btree_gist` extension.
- [ ] Integration test: inserting two `ScheduleTemplate` rows whose
      `[validFrom, validTo)` overlap for the same `(user_id, view)` is rejected
      by the database; the same rows with `view` differing are accepted.
- [ ] Integration test: a gap between two versions is accepted (overview §3.2 —
      holes are legal).
- [ ] Seed script runs against a clean database and produces the T-001 fixture
      scenario. The fixture document is the single source for that scenario —
      the seed is derived from it, and the T-005 test expectations are too;
      neither restates it independently.
- [ ] `drizzle-kit migrate` documented in `README.md` as an explicit deploy step.

## Notes
